[project]
name = "ytdl"
version = "0.1.0"
description = "Cli Youtube downloader"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pytubefix>=10.3.6",
]

[project.scripts]
ytdl = "ytdl:download_video"

